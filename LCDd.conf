description "LCDd - listen to a port and display info on an LCD display"

start on filesystem
stop on runlevel [016]

respawn

pre-start exec test -x /usr/sbin/LCDd || { stop; exit 0; }

exec /usr/sbin/LCDd -f -s 1 -c /etc/LCDd.conf
