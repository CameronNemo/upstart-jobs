description "busybox-klogd - Busybox kernel logger"

start on filesystem
stop on runlevel [016]

expect fork
respawn
reload signal 0

pre-start script
    test -x /sbin/klogd || { stop; exit 0; }
end script

exec /sbin/klogd
