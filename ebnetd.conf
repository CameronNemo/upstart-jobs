description "ebnetd - EBNET protocol server"

start on filesystem
stop on runlevel [016]

pre-start script
    test -d /run/ebnetd || {
        mkdir /run/ebnetd
        chown ebnetd:nogroup /run/ebnetd
    }
end script
