description "fso-deviced - freesmartphone.org device daemon"

start on started dbus
stop on stopping dbus

respawn

pre-start exec test -x /usr/sbin/fsodeviced || { stop; exit 0; }
exec /usr/sbin/fsodeviced
