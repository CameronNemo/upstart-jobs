description "kanla - small-scale alerting daemon"

start on filesystem
stop on runlevel [016]

# TODO: readiness
respawn

setuid kanla
setgid kanla

pre-start exec test -x /usr/bin/kanla || { stop; exit 0; }

exec /usr/bin/kanla
