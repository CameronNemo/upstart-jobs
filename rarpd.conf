description "rarpd - Reverse Address Resolution Protocol (RARP) daemon"
author "Cameron Norman <camerontnorman@gmail.com>"

expect fork
respawn

pre-start exec test -x /usr/sbin/rarpd || { stop; exit 0; }
exec /usr/sbin/rarpd -e -v
