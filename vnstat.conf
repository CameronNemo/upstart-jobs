description "vnstat - lightweight network traffic monitor"

start on filesystem and net-device-up INTERFACE!=lo
stop on runlevel [016] or deconfiguring-networking

# TODO: readiness
respawn

setuid vnstat
setgid vnstat

pre-start exec test -x /usr/sbin/vnstatd || { stop; exit 0; }
exec /usr/sbin/vnstatd -n
